import React from "react";
import { Grid } from "@mui/material";

const LocationList = () => {
  return (
    <Grid container sx={{}}>
      LocationList
    </Grid>
  );
};

export default LocationList;
